export const bold = (text: unknown) => `<b>${String(text)}</b>`
export const italic = (text: unknown) => `<i>${String(text)}</i>`
export const underline = (text: unknown) => `<u>${String(text)}</u>`
export const code = (text: unknown) => `<code>${String(text)}</code>`
